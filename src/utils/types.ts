export interface UserData {
    roleId: number;
    orgId: number;
    fname: string;
    lname: string;
    email: string;
    password: string;
}

export interface OrganizationData {
    name: string;
}

export interface RoleData {
    name: string;
}